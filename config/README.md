# config

## Basic

- `development.yaml`
- `test.yaml`

## Production

- `production.yaml` (.gitignore includes)

## Local dev

- `local.yaml` (.gitignore includes)
- `*.local.yaml` (.gitignore includes)
