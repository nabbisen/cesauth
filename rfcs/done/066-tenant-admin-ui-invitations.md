# RFC 066 — Tenant admin UI: invitation page

**Status**: Implemented  
**Size**: Medium

Add `invitation_list_page(principal, tenant, invitations)` to
`cesauth-ui::tenant_admin`. Shows pending invitations, allows revoke.
Add "Invite user" button to the users page.
