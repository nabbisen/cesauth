{
  "name": "cesauth-tooling",
  "private": true,
  "description": "Pinned tooling for cesauth (RFC 138). Run npm ci at the repository root, then call node_modules/.bin/wrangler; npx would bypass the pin.",
  "devDependencies": {
    "wrangler": "4.131.2"
  }
}
